# Extra Python packages installed at image build (see Dockerfile).
# Add pins required by modules under ./addons/.
# paramiko==2.7.2  # for auto_backup module
pandas==2.2.3
beautifulsoup4==4.13.3
click-odoo-contrib==1.23.1
